# MATRIZ-SEGURANCA-ESCRITA-SUPABASE.md

Versão: 3.5  
Status: Oficial  
Última atualização: 18/08/2026  
Projeto: Vibe

---

# Matriz Oficial de Segurança de Escrita no Supabase

Este documento define quais operações de leitura e escrita no Supabase estão autorizadas, bloqueadas, planejadas ou em validação no Vibe.

Ele é a referência oficial para qualquer alteração envolvendo `READ`, `INSERT`, `UPDATE` ou `DELETE`.

Nenhuma nova operação de escrita deve ser implementada sem respeitar esta matriz e sem atualizar seu status quando houver mudança de fase.

---

# Objetivo

Garantir que toda operação realizada no Supabase preserve:

- integridade dos dados;
- segurança;
- rastreabilidade;
- regras de negócio;
- compatibilidade entre módulos;
- possibilidade de validação antes e depois da gravação.

Este documento complementa:

- `SECURITY.md`;
- `BUSINESS_RULES.md`;
- `DEVELOPMENT.md`;
- `technical/PERFIS-PERMISSOES.md`.

---

Documento vivo para controlar o que está `LIBERADO`, `BLOQUEADO`, `FUTURO` ou `EM TESTE` no caminho de escrita real do Vibe.

> Atualize este arquivo sempre que uma nova fase de escrita for validada.

## Convenções

- `READ`: leitura disponível no app.
- `UPDATE`: alteração pontual de campos existentes.
- `INSERT`: criação de novos registros.
- `DELETE`: remoção física.
- `LIBERADO`: já validado e em uso.
- `BLOQUEADO`: explicitamente proibido nesta fase.
- `FUTURO`: planejado para próximas fases, ainda não habilitado.
- `EM TESTE`: em validação controlada, com escopo reduzido.

## Cadastros

| Tabela | Campo | Operações | Status | Motivo da decisão | Risco | Validação pós-gravação | Data/fase da liberação | Observações técnicas |
|---|---|---|---|---|---|---|---|---|
| `public.clientes` | Campos cadastrais, fiscais, crédito e observações em whitelist da tela de cadastro | `READ`, `INSERT`, `UPDATE` | `LIBERADO` | Escrita real expandida para criação e edição completa no módulo Cadastros, mantendo whitelist explícita e bloqueio de `id_cliente` em edição. | Médio/alto | `select id_cliente, nome, documento, categoria, nome_vendedor, tipo_pessoa, limite_credito, credito from public.clientes where id_cliente = :id_cliente;` | `2026-05-27` / Fase 2 | `DELETE` permanece bloqueado. `id_cliente` é obrigatório no `INSERT` e somente leitura no `UPDATE`. |
| `public.clientes` | Demais campos cadastrais, fiscais, crédito, risco, auditoria, `apelido`, endereços, contatos e vínculos | `READ` | `LIBERADO` | A leitura já está em uso; escrita continua travada para campos fora da whitelist operacional. | Médio/alto | `select * from public.clientes where id_cliente = :id_cliente;` | `2026-05-23` / Base read-only | Inclui campos não validados para escrita e relacionamentos que continuam sem `UPDATE`, `INSERT` ou `DELETE`. |
| `public.clientes` | Demais campos cadastrais, fiscais, crédito, risco, auditoria, `apelido`, endereços, contatos e vínculos | `UPDATE`, `INSERT`, `DELETE` | `BLOQUEADO` | Não faz parte da fase validada. Campos sensíveis e relacionamentos continuam travados. | Alto/Crítico | Conferência somente leitura antes e depois. | Permanente nesta fase | Liberar por campo apenas após validação específica e nova decisão explícita. |
| `public.clientes` | Qualquer campo | `DELETE` | `BLOQUEADO` | Remoção física não é permitida nesta etapa do projeto. | Crítico | Não aplicável. | Permanente | Mantido bloqueado por regra de segurança do projeto. |
| `public.enderecos` | Campos operacionais de endereço | `READ` | `LIBERADO` | A leitura já é usada no detalhe de cadastro e na proposta. | Baixo | `select * from public.enderecos where id_cliente = :id_cliente order by id;` | `2026-05-23` / Read-only | Inclui CEP, logradouro, número, complemento, bairro, cidade, UF e tipo. |
| `public.enderecos` | Campos operacionais de endereço em whitelist (`id_cliente`, `cep`, `endereco`, `numero`, `complemento`, `bairro`, `cidade`, `uf`, `tipo_endereco`, `obs`, `Latitude`, `longitude`, `distancia`) | `READ`, `INSERT`, `UPDATE` | `LIBERADO` | Escrita real liberada no fluxo de criação e edição de cadastro com distinção entre registros existentes (`UPDATE`) e novos (`INSERT`). | Médio | `select id, id_cliente, cep, endereco, numero, cidade, uf, tipo_endereco from public.enderecos where id_cliente = :id_cliente order by id;` | `2026-05-27` / Fase 2 | Não há remoção física nesta fase. |
| `public.enderecos` | Qualquer campo | `DELETE` | `BLOQUEADO` | Apagar endereço pode quebrar NF, proposta e entrega. | Alto | Não aplicável. | Permanente | Só considerar soft delete se houver decisão específica. |
| `public.clientes_socios` | Campos de vínculo comercial | `READ` | `LIBERADO` | Usado para vínculos e autorizados nas telas de detalhe e proposta. | Baixo | `select * from public.clientes_socios where id_cliente_principal = :id_cliente or id_cliente_socio = :id_cliente;` | `2026-05-23` / Read-only | Representa vínculos comerciais, não apenas sócios. |
| `public.clientes_socios` | Campos de vínculo comercial (`id_cliente_principal`, `id_cliente_socio`, `tipo_relacao`) | `READ`, `INSERT`, `UPDATE` | `LIBERADO` | Vínculos comerciais internos liberados para criação e edição com busca de cadastro existente e bloqueio de auto-vínculo/duplicidade na UI. | Médio/alto | `select id, id_cliente_principal, id_cliente_socio, tipo_relacao from public.clientes_socios where id_cliente_principal = :id_cliente;` | `2026-05-27` / Fase 2 | Sócios fiscais externos (API CNPJ) continuam fora desta tabela. |
| `public.clientes_socios` | Qualquer campo | `DELETE` | `BLOQUEADO` | Remover vínculo pode quebrar histórico comercial. | Alto | Não aplicável. | Permanente | Manter bloqueado até decisão explícita. |
| `public.contatos` | Qualquer campo | `READ` | `LIBERADO` | A leitura já é usada no detalhe e na proposta. | Baixo | `select * from public.contatos where id_cliente = :id_cliente;` | `2026-05-23` / Read-only | Tabela depende de `authenticated` real para escrita. |
| `public.contatos` | Campos em whitelist (`id_cliente`, `nome_contato`, `cargo`, `whats`, `e_mail`) | `READ`, `INSERT`, `UPDATE` | `LIBERADO` | Fluxo de cadastro passou a salvar contatos com whitelist e validação de `nome_contato` obrigatório para registros adicionados. | Alto | `select id, id_cliente, nome_contato, cargo, whats, e_mail from public.contatos where id_cliente = :id_cliente;` | `2026-05-27` / Fase 2 | Em caso de bloqueio de permissão/RLS, cliente principal não é desfeito e a UI informa falha parcial. |
| `public.contatos` | Qualquer campo | `DELETE` | `BLOQUEADO` | Remoção física permanece proibida nesta etapa. | Alto | Não aplicável. | Permanente | Exclusão continua fora de escopo. |

## Produtos

| Tabela | Campo | Operações | Status | Motivo da decisão | Risco | Validação pós-gravação | Data/fase da liberação | Observações técnicas |
|---|---|---|---|---|---|---|---|---|
| `public.produtos` | Campos descritivos, comerciais, operacionais e fiscais exibidos no catálogo | `READ` | `LIBERADO` | Listagem e detalhe de Produtos já assumem `public.produtos` como fonte real, com leitura complementar de fotos/variações quando disponível. | Baixo | `select id_produto, nomeReal, formato, descricao, personalizacao, apelidos, ativo, is_estoque, is_variacao, peso from public.produtos order by id_produto asc;` | `2026-05-28` / Read-only real | Peso interpretado como gramas. Fallback visual permanece quando dependências de mídia/variações não respondem. |
| `public.produtos` | Whitelist operacional, valores, fiscal e produção: `nomeReal`, `categoria`, `formato`, `descricao`, `personalizacao`, `apelidos`, `ativo`, `is_estoque`, `is_variacao`, `nivelSeg`, `fraseCons`, `prazo`, `peso`, `valorUnt`, `valorFixo`, `valor_custo`, `cod_beneficio`, `ncm`, `descri_ncm`, `cest`, `origem`, `cod_origem`, `cod_bar`, `und_medida`, `is_multiplo`, `cfop_interno`, `cfop_interestadual`, `unidade_comercial`, `unidade_tributavel`, `icms_origem`, `icms_situacao_tributaria`, `pis_situacao_tributaria`, `cofins_situacao_tributaria`, `informacoes_fiscais`, `id_formato`, `id_modelo_cor`, `quantidade_minima_venda`, `tipo_blocagem` | `UPDATE` | `LIBERADO` | Edição real expandida para valores comerciais, dados fiscais e campos de produção (formato, cor/modelo, quantidade mínima e tipo de blocagem), usando `id_produto` como chave segura. | Alto | SQL de validação do produto com todos os campos fiscais, comerciais e de produção. | `2026-06-14` / Catálogo Produção | `peso` permanece em gramas. Campos de catálogo `id_formato` e `id_modelo_cor` salvam chaves inteiras, validadas por chaves estrangeiras. |
| `public.produtos` | `id`, `created_at`, `id_produto` no `UPDATE` | `UPDATE` | `BLOQUEADO` | Campos de identidade, auditoria e chave operacional não devem ser alterados pela tela. | Alto | Somente leitura comparativa. | Permanente nesta etapa | `id_produto` permanece somente leitura na edição. |
| `public.produtos` | Whitelist operacional, valores e fiscal com `id_produto` obrigatório | `INSERT` | `LIBERADO` | Criação real expandida com validação manual de `id_produto`, checagem de duplicidade antes do `INSERT` e sem `upsert`. | Alto | SQL de validação do produto com todos os campos fiscais e comerciais liberados. | `2026-05-28` / Fase 4 | `id_produto` é informado pelo usuário e deve ser único. Campo vazio vira `null` quando aplicável. |
| `public.produtos` | `id_gabarito`, `setor_pcp` | `INSERT`, `UPDATE` | `EM TESTE` | Integração de gabarito padrão e setor PCP no cadastro de produtos. Requer migração de DDL do banco executada. | Alto | SQL de validação do produto com novos campos operacionais. | `2026-06-14` / Boletim OS | Fase em teste para integrar com PCP e numerações de produção. |
| `public.produtos` | Qualquer campo | `DELETE` | `BLOQUEADO` | Risco operacional alto para catálogo usado por propostas. | Crítico | Não aplicável. | Permanente | Manter bloqueado. |
| `public.fotosProdutos` | `id`, `nomeProduto`, `imagensURL`, `idProduto` | `READ` | `LIBERADO` | Galeria do produto já lê fotos reais vinculadas por `idProduto`. | Baixo/médio | `select * from public."fotosProdutos" where "idProduto" = :id_produto order by id desc;` | `2026-05-28` / Fase 4 | A coluna real é `idProduto`, não `id_produto`. |
| `public.fotosProdutos` | `nomeProduto`, `imagensURL`, `idProduto` após upload no bucket `e-deal` | `INSERT` | `LIBERADO` | Upload de nova imagem salva arquivo no Supabase Storage e só depois registra a referência na tabela. | Médio | `select * from public."fotosProdutos" where "idProduto" = :id_produto order by id desc;` | `2026-05-28` / Fase 4 | Não usa `upsert` e não apaga imagens antigas. Bucket esperado: `e-deal`, pasta `produtos/`. |
| `public.fotosProdutos` | Qualquer campo | `UPDATE` | `BLOQUEADO` | Alteração de metadados/foto principal fica para fase própria. | Médio | Não aplicável. | Permanente nesta etapa | Apenas adicionar nova foto está liberado. |
| `public.fotosProdutos` | Qualquer campo | `DELETE` | `BLOQUEADO` | Remoção pode quebrar galeria visual e histórico. | Médio | Não aplicável. | Permanente nesta etapa | Bloqueio inicial por segurança. |
| `public.produto_variacoes` | Vínculo de variação e flags | `READ` | `LIBERADO` | Módulo de variações de produto implementado para leitura dos vínculos operacionais. | Médio | `select * from public.produto_variacoes where id_produto = :id_produto;` | `2026-05-28` / Fase 5 | Mapeia vínculos com produtos para alimentar orçamentos e Maestro. |
| `public.produto_variacoes` | Vínculo de variação e flags em whitelist (`id_produto`, `id_variacao`, `nome`, `is_obrigatorio`, `is_multiplo`) | `UPDATE`, `INSERT` | `LIBERADO` | Escrita de vínculos e configurações de variações por produto liberada com cópia do nome e sincronização em lote. | Alto | `select * from public.produto_variacoes where id_produto = :id_produto order by id desc;` | `2026-05-28` / Fase 5 | Não usa upsert. `id_produto` e `id_variacao` não podem ser alterados no update. |
| `public.produto_variacoes` | Vínculo de variação | `DELETE` | `LIBERADO` | Remoção física permitida exclusivamente para desvincular grupos de variação do produto (tabela N-N). | Alto | Conferência visual na edição do produto. | `2026-05-28` / Fase 5 | DELETE físico restrito a desvincular registros de relacionamento. |
| `public.variacoes` | Grupo global de variação | `READ` | `LIBERADO` | Leitura existe para alimentar a listagem global e vinculação no produto. | Baixo | `select * from public.variacoes order by nome;` | `2026-05-28` / Fase 5 | Catálogo global compartilhado. |
| `public.variacoes` | Nome, descrição, ativo em whitelist | `UPDATE`, `INSERT` | `LIBERADO` | Criação e edição do banco global de variações no painel administrativo de variações globais. | Alto | `select * from public.variacoes where id_variacao = :id;` | `2026-05-28` / Fase 5 | DELETE físico é bloqueado por regra e na camada de serviço (usa is_ativo=false). |
| `public.tipos_variacoes` | Opções globais do grupo | `READ` | `LIBERADO` | Usado para compor escolhas de variações na listagem e produto. | Baixo | `select * from public.tipos_variacoes order by id;` | `2026-05-28` / Fase 5 | Fonte de opções e possíveis acréscimos/pesos. |
| `public.tipos_variacoes` | Opções globais em whitelist (`id_variacao`, `variacao`, `v_extra`, `ref`, `peso`, `is_ativo`) | `UPDATE`, `INSERT` | `LIBERADO` | Inserção e edição de opções pertencentes ao grupo global de variação. | Alto | `select * from public.tipos_variacoes where id = :id;` | `2026-05-28` / Fase 5 | Peso salvo em gramas. DELETE físico bloqueado na camada de serviço (usa is_ativo=false). |

## Propostas

| Tabela | Campo | Operações | Status | Motivo da decisão | Risco | Validação pós-gravação | Data/fase da liberação | Observações técnicas |
|---|---|---|---|---|---|---|---|---|
| `public.propostas` | `id_cliente`, `cliente`, `empresa`, `vendedor`, `valor`, `valor_total`, `obs_proposta`, `texto_whatsapp`, `frete_escolhido`, `valor_frete`, `contato`, `cep`, `is_avulso` | `READ`, `INSERT`, `UPDATE` | `LIBERADO` | Criação e edição de propostas pelo fluxo oficial, com whitelist e validações comerciais. | Alto | `select id_int, cliente, valor_total, status_interno, frete_escolhido from public.propostas where id_int = :id_int;` | `2026-05-28` / Fase 6 | A gravação em lote concilia itens e variações. Não autoriza escrita genérica fora do service oficial. |
| `public.propostas` | `status_interno` | `UPDATE` | `EM TESTE` | Transição manual controlada, com matriz central, permissão, feature flag e auditoria. | Crítico | Conferir status anterior e posterior por `id_int` e o registro de auditoria. | Fase 4A / homologação controlada | Não autoriza automação, escrita em massa nem transição fora da matriz oficial. |
| `public.propostas` | `is_prd_aprovado` | `UPDATE` | `LIBERADO` | Liberação e retirada manual da fila de Produção pelo fluxo oficial. | Crítico | `select id_int, is_prd_aprovado, status_interno from public.propostas where id_int = :id_int;` | Fluxo de Produção vigente | Exige permissão, confirmação, validação operacional e auditoria. Não altera `status_interno` automaticamente. |
| `public.propostas` | Qualquer campo | `DELETE` | `BLOQUEADO` | Proposta é registro crítico de histórico comercial e financeiro. | Crítico | Não aplicável. | Permanente | Mantido bloqueado. |
| `public.produtos_proposta` | `id_int`, `id_produto`, `nome_produto`, `modelo_descri`, `valor_unt`, `qtd`, `fixo`, `valor_sub_total`, `peso_uni`, `peso_total`, `peso_base`, `peso_extra`, `valor_base`, `valor_extra`, `ncm` | `READ`, `INSERT`, `UPDATE` | `LIBERADO` | Persistência dos itens da proposta com recálculos comerciais e de peso base + extra de variações. | Alto | `select * from public.produtos_proposta where id_int = :id_int;` | `2026-05-28` / Fase 6 | Peso unitário e peso total calculados em gramas no backend/client. |
| `public.produtos_proposta` | Vínculos de itens na proposta | `DELETE` | `LIBERADO` | Permitida a remoção física apenas para fins de conciliação em lote dos itens ao atualizar a proposta. | Alto | Conferência visual na tela de edição do orçamento. | `2026-05-28` / Fase 6 | Restrito aos itens que foram desassociados no formulário. |
| `public.produtos_proposta_variacao` | `id_produto_proposta`, `id_variacao`, `id_tipo_variacao`, `nome_variacao`, `v_extra`, `peso_uni` | `READ`, `INSERT` | `LIBERADO` | Snapshot estático/histórico da escolha de variações do produto na proposta (preservando valores e pesos extras). | Médio | `select * from public.produtos_proposta_variacao where id_produto_proposta = :id_item;` | `2026-05-28` / Fase 6 | Gravado sempre após o item correspondente. Não há atualização direta (apenas inserção no fluxo). |
| `public.produtos_proposta_variacao` | Escolha de variação | `DELETE` | `LIBERADO` | Permitida a remoção física exclusiva para conciliação em lote ao atualizar ou remover o item da proposta. | Médio | Conferência visual na proposta. | `2026-05-28` / Fase 6 | Deleta todas as variações do item antes de reinserir as opções selecionadas atuais. |
| `public.produtos_proposta_variacao` | Qualquer exclusão direta fora da conciliação oficial | `DELETE` | `BLOQUEADO` | Impede remoção isolada fora do fluxo de gravação estruturado. | Alto | Não aplicável. | Permanente | O `DELETE` liberado acima vale exclusivamente para a conciliação oficial do item da proposta. |
| `public.cotacao_frete` | Seleção e observação de frete | `READ`, `UPDATE`, `INSERT` | `LIBERADO` | Escrita controlada liberada para o cálculo de frete e consolidação nas propostas. | Alto | `select * from public.cotacao_frete where id_int = :id_int order by id;` | `2026-05-30` / Fase 6 | Tabela com triggers de recálculo; o frete escolhido atualiza o total final. |
| `public.cotacao_frete` | Qualquer campo | `DELETE` | `BLOQUEADO` | Excluir frete diretamente pode desestruturar o cálculo total da proposta. | Crítico | Não aplicável. | Permanente | Bloqueio por padrão. |
| `public.cotacao_frete` | **Efeito colateral de QUALQUER escrita** (`INSERT`/`UPDATE`/`DELETE`) — vale para as duas linhas acima | — | `ATENÇÃO` | Três triggers `FOR EACH ROW` disparam por escrita nesta tabela, e **dois têm efeito real sobre `public.propostas`**: `trg_recalc_after_frete` (`AFTER INSERT OR UPDATE` → `recalcular_proposta_v3`, `void`) faz `UPDATE propostas SET valor, volume, valor_total`; `trg_frete_sync_financeiro` (`AFTER INSERT OR DELETE OR UPDATE` → `atualizar_status_financeiro_proposta`, `void`) faz `UPDATE propostas SET status_interno` para `NOVO`/`AGUARDANDO`/`APROVADO`/`CANCELADO` conforme `pagamentos_v2`. Apenas `tg_recalc_frete_v4` (→ `recalcular_proposta_v4`, `RETURNS TABLE`) é no-op. **A migration `20260804_recalc_valor_total_propostas.sql` documenta os triggers de recálculo como no-op — isso só vale para o `v4`; os outros dois não estão nessa migration e existem no banco de produção.** | Crítico | `select tgname, pg_get_triggerdef(t.oid) from pg_trigger t join pg_class c on c.oid=t.tgrelid where not t.tgisinternal and c.relname='cotacao_frete';` + conferir `valor_total` e `status_interno` da proposta antes/depois. | Verificado no banco vivo em `2026-08-18` | Consequência prática: escrever aqui em um pedido que já está em `EXPEDICAO`/`A RETIRAR`/`EM TRANSITO` **reescreve `status_interno` pelos pagamentos e tira o pedido do funil logístico**. Por isso a Expedição trata `cotacao_frete` como somente-leitura e grava execução em `public.expedicoes` (ver `docs/business/EXPEDICAO.md` §2). Qualquer fluxo novo que precise recotar frete deve tratar este efeito antes. |
| `public.desconto_proposta` | Desconto comercial | `READ`, `UPDATE`, `INSERT` | `LIBERADO` | Escrita de descontos comerciais da proposta liberada sob validação de perfil (admin/gerente para desconto geral). | Alto | `select * from public.desconto_proposta where id_int = :id_int;` | `2026-05-30` / Fase 7 | Deve respeitar as regras e perfis comerciais para aplicação de descontos. |
| `public.desconto_proposta` | Qualquer campo | `DELETE` | `BLOQUEADO` | Remover desconto altera total e histórico comercial. | Alto | Não aplicável. | Permanente | Manter bloqueado. |
| `public.pagamentos_v2` | Campos retornados pelas consultas financeiras autorizadas | `READ` | `LIBERADO` | Leitura usada por cobranças, conferência e Maestro conforme sessão, perfil e RLS. | Alto | `select * from public.pagamentos_v2 where id_int = :id_int;` | Fase Financeira | Não expor tokens, payloads sensíveis ou dados de pagamento fora do fluxo autorizado. |
| `public.pagamentos_v2` | Payload estrito definido pelos services oficiais de criação e confirmação de cobrança | `INSERT`, `UPDATE` | `LIBERADO` | Escrita permitida somente por fluxos financeiros homologados, com validação, permissão, confirmação e retorno real da operação. | Crítico | Conferir o registro por `id_int` e o estado financeiro após a operação. | Fase Financeira | Não significa liberação de qualquer campo. Escrita direta ou genérica pelo frontend permanece proibida. |
| `public.pagamentos_v2` | Qualquer escrita fora dos services financeiros oficiais | `INSERT`, `UPDATE` | `BLOQUEADO` | Evita alteração genérica de cobrança, confirmação ou recebimento. | Crítico | Não aplicável. | Permanente | Nova modalidade exige diagnóstico, whitelist, integração e atualização desta matriz. |
| `public.pagamentos_v2` | `status`, `motivo_cancela` — exclusivamente para o cancelamento de cobrança já paga | `UPDATE` | `LIBERADO` | Exceção autorizada e nomeada ao bloqueio geral de cobrança paga (ver `business/CANCELAMENTO-COBRANCAS.md`), restrita a super admin pela rota `POST /api/cobrancas/cancelar-pago`. Não é escrita genérica: exige motivo de catálogo e destino definido para o valor. | Crítico | `select id, status, motivo_cancela, paid_at, data_confirmacao from public.pagamentos_v2 where id = :id;` | `2026-08-11` / Exceção super admin (cancelar-pago) | Sem chamada a provedor externo. Bloqueado quando a proposta está em status operacional (exceto `REVISAO ATENDENTE`); exige confirmação explícita para confirmação de mês anterior; crédito lançado via `mc_ajuste_avulso_criar` ANTES do cancelamento (falha na conta corrente impede a gravação). As rotas `cancelar-externo` e `cancelar-boleto` continuam bloqueando cobrança paga sem exceção. |
| `public.pagamentos_v2` | Qualquer campo | `DELETE` | `BLOQUEADO` | Remoção de registros de cobrança e pagamentos é estritamente proibida por conformidade fiscal e financeira. | Crítico | Não aplicável. | Permanente | Bloqueado. |
| `public.boletos` | Campos de consulta de títulos, vencimentos e liquidação | `READ` | `LIBERADO` | Fonte oficial de Contas a Receber e boletos. | Alto | Consultar pelo identificador do título, `id_int` e cliente. | Financeiro | Não expor linha digitável, código de barras ou URL fora do fluxo autorizado. |
| `public.boletos` | Atualização de status ou cancelamento pelo backend financeiro oficial | `UPDATE` | `LIBERADO` | Permitido somente quando a integração oficial confirmar a operação externa ou financeira. | Crítico | Reconsultar o título e validar estado, liquidação e motivo. | Financeiro | Frontend não atualiza diretamente quando houver integração externa. |
| `public.boletos` | Escrita direta ou genérica fora do backend oficial | `INSERT`, `UPDATE` | `BLOQUEADO` | Evita criação ou alteração de títulos sem integração, validação e auditoria. | Crítico | Não aplicável. | Permanente | Nova operação exige whitelist e atualização desta matriz. |
| `public.boletos` | Qualquer campo | `DELETE` | `BLOQUEADO` | Títulos financeiros exigem preservação histórica. | Crítico | Não aplicável. | Permanente | Preferir cancelamento lógico. |


## Pedidos / Produção

| Tabela | Campo | Operações | Status | Motivo da decisão | Risco | Validação pós-gravação | Data/fase da liberação | Observações técnicas |
|---|---|---|---|---|---|---|---|---|
| `public.pedidos` | `id_int`, `id_vendedor`, `id_cliente`, `status_pedido`, `status_pagamento`, `status_arte`, `status_producao`, `status_expedicao`, `descricao`, `valor_total`, `forma_pagamento`, `obs`, `data_pedido` | `INSERT` | `LIBERADO` | Registro controlado do pedido pai a partir da abertura de Boletim de Entrada (Abertura de OS) com validações severas de elegibilidade. | Médio | `select * from public.pedidos where id_int = :id_int;` | `2026-06-14` / Boletim OS | Risco médio; escrita restrita ao INSERT do registro pai, com `id_cliente` nulo. |
| `public.pedidos` | `obs` | `UPDATE` | `LIBERADO` | Habilitada edição do Boletim/OS existente. As orientações textuais (designer, instruções de layout, briefing de impressão/acabamento) são concatenadas e salvas em `obs` no formato bracketed estruturado. | Baixo | `select obs from public.pedidos where id_int = :id_int;` | `2026-06-16` / Edição OS | Risco baixo; as regras do ERP garantem que apenas esse campo textual seja atualizado no modo edição. |
| `public.pedidos` | Qualquer campo | `DELETE` | `BLOQUEADO` | Remoção de pedidos é proibida por conformidade fiscal e integridade de produção. | Crítico | Não aplicável. | Permanente | Mantido bloqueado por segurança. |
| `public.pedidos_modelos` | `id_int`, `id_pedido`, `id_produto_proposta_origem`, `nome_modelo`, `descricao`, `quantidade`, `tipo_numeracao`, `numeracao_inicio`, `numeracao_fim`, `obs_impressao`, `status_arte`, `status_producao`, `ordem` | `INSERT` | `LIBERADO` | Inserção atômica em lote de modelos/lotes operacionais associados ao pedido criado no Boletim de Entrada. | Médio/Alto | `select * from public.pedidos_modelos where id_int = :id_int;` | `2026-06-15` / Boletim OS | RLS autenticada com validação estrutural. Quantidades validadas na UI e anti-duplicidade no service. |
| `public.pedidos_modelos` | Qualquer campo | `UPDATE`, `DELETE` | `BLOQUEADO` | Edição e exclusão de modelos/lotes salvos ainda não estão habilitadas nesta etapa. | Alto | Não aplicável. | Permanente nesta etapa | Mantido bloqueado por segurança contra loops e perda de dados. |
| `public.pedidos_artes` | `id_int`, `id_modelo`, `versao`, `nome_arquivo`, `storage_bucket`, `storage_path`, `url_arquivo`, `tipo_arquivo`, `mime_type`, `tamanho_bytes`, `status`, `enviado_por`, `enviado_por_uid` | `INSERT` | `LIBERADO` | Upload controlado de artes da Versão 1 a partir da Ficha de OS (Aba Artes/Aprovação), associado ao modelo correspondente. | Médio/Alto | `select * from public.pedidos_artes where id_int = :id_int;` | `2026-06-15` / Ficha OS Artes | Restrição estrita a versao = 1 e bloqueio de novos uploads se já houver arte no modelo. |
| `public.pedidos_artes` | Qualquer campo | `UPDATE`, `DELETE` | `BLOQUEADO` | Alteração e exclusão de artes não são permitidas nesta etapa. | Crítico | Não aplicável. | Permanente nesta etapa | Mantido bloqueado por segurança. |


## Chat Interno e Pendências

| Tabela ou recurso | Campo ou escopo | Operações | Status | Motivo da decisão | Risco | Validação pós-gravação | Data/fase da liberação | Observações técnicas |
|---|---|---|---|---|---|---|---|---|
| `public.propostas_chat` | Mensagem, tipo, setor, autor, `id_int`, anexos e visibilidade conforme o service oficial | `READ`, `INSERT` | `LIBERADO` | Timeline operacional compartilhada da proposta. | Médio | Consultar mensagens pelo mesmo `id_int` e validar autor, tipo e anexos. | Chat Interno | Mensagens são registros históricos. Não executar regra de negócio a partir do texto. |
| `public.propostas_chat` | Qualquer campo persistido | `UPDATE`, `DELETE` | `BLOQUEADO` | Evita reescrita ou remoção do histórico operacional. | Alto | Não aplicável. | Permanente | Correções devem usar novo evento ou fluxo específico, não edição silenciosa. |
| `public.propostas_chat_mentions` | Menções estruturadas e marcação de leitura | `READ`, `INSERT`, `UPDATE` | `LIBERADO` | Notificações por usuário e controle de leitura. | Médio | Validar `chat_id`, usuário mencionado, `id_int` e `read_at`. | Chat Interno | `UPDATE` limitado à marcação de leitura pelo fluxo oficial. |
| `public.propostas_chat_mentions` | Exclusão direta | `DELETE` | `BLOQUEADO` | Preserva rastreabilidade e evita remoção isolada. | Alto | Não aplicável. | Permanente | `ON DELETE CASCADE` da FK não autoriza deleção direta via cliente. |
| `public.propostas_pendencias` | Campos operacionais autorizados pelo service e RLS | `READ`, `INSERT`, `UPDATE` | `LIBERADO` | Gestão de pendências por criador, responsável, setor e empresa. | Alto | Consultar por `id_int`, responsável, status, empresa e setor após a operação. | Fase 6D | Atualização deve respeitar transições, escopo e auditoria. |
| `public.propostas_pendencias` | Qualquer campo | `DELETE` | `BLOQUEADO` | Pendências são encerradas ou canceladas, não excluídas. | Crítico | Não aplicável. | Permanente | RLS sem política de `DELETE`. |
| Storage `chat-ideal` | Upload de anexos pelo fluxo oficial | `READ`, `INSERT` | `LIBERADO` | Anexos do Chat Interno e fluxos operacionais autorizados. | Alto | Confirmar upload, MIME, tamanho, path e vínculo antes de gravar a mensagem. | Chat Interno | Não assumir extensões ou limites sem confirmar a implementação. |
| Storage `chat-ideal` | Alteração ou remoção direta | `UPDATE`, `DELETE` | `BLOQUEADO` | Evita quebra de histórico e links de anexos. | Alto | Não aplicável. | Permanente | Nova política de retenção exige decisão específica. |


## Perfis e Permissões

| Tabela | Campo | Operações | Status | Motivo da decisão | Risco | Validação pós-gravação | Data/fase da liberação | Observações técnicas |
|---|---|---|---|---|---|---|---|---|
| `public.perfis` | Todos os campos (`id`, `slug`, `nome`, `descricao`, `permissoes`, `ativo`, `created_at`, `updated_at`) | `READ` | `LIBERADO` | Catálogo de perfis oficial lido pelo `AuthProvider` para enriquecimento dinâmico de privilégios de acesso e permissões ativas. | Baixo | `select * from public.perfis order by slug;` | `2026-06-10` / Fase 1 | Apenas leitura liberada. |
| `public.perfis` | Qualquer campo | `UPDATE`, `INSERT`, `DELETE` | `BLOQUEADO` | A existência do editor visual não autoriza persistência no catálogo de segurança. | Crítico | Não aplicável. | Permanente nesta fase | Tabela crítica. RPC, formulário ou script só podem ser liberados após diagnóstico e atualização explícita desta matriz. |
| `public.usuarios` | `id_perfil` | `READ` | `LIBERADO` | Utilizado para cruzamento e enriquecimento do perfil do usuário autenticado no app. | Baixo | `select id_perfil from public.usuarios where id = :auth_id;` | `2026-06-10` / Fase 1 | Coluna FK nullable para suportar fallback de dados legados. |
| `public.usuarios` | `id_perfil` | `UPDATE`, `INSERT` | `BLOQUEADO` | A atribuição de perfil ainda não possui autorização de escrita nesta matriz. | Alto | Não aplicável. | Permanente nesta fase | A UI pode existir, mas deve permanecer sem persistência real até nova decisão e validação de RLS. |

## Conta Corrente (Pendências Financeiras)

> Domínio da diferença financeira **após proposta paga**. Não confundir com `public.boletos` (Contas a Receber) nem com `public.pagamentos_v2` (pagamentos). Fase 1 **aplicada em produção** — confirmado em 11/08/2026 (RPCs e `public.conta_corrente_pendencias` existem e estão em uso). Ver `business/CONTA-CORRENTE-FASE-1-PREPARACAO.md` e `supabase/migrations/20260721_conta_corrente_fase1.sql`.

| Tabela | Campo | Operações | Status | Motivo da decisão | Risco | Validação pós-gravação | Data/fase da liberação | Observações técnicas |
|---|---|---|---|---|---|---|---|---|
| `public.conta_corrente_pendencias` | Estado da pendência (identidade, direção, motivo, valores, status) | `READ` | `FUTURO` | Fonte operacional do saldo da pendência. Leitura autenticada; escopo fino na aplicação. | Alto | `select id, id_int, id_cliente, direcao, valor_original, valor_saldo, valor_reservado, status from public.conta_corrente_pendencias where id_int = :id_int;` | `2026-08-11` / Fase 1 CC (aplicada) | Tabela nova. RLS habilitada, sem policy de escrita. Existência confirmada em produção em 11/08/2026. |
| `public.conta_corrente_pendencias` | Alteração de saldo/estado | `INSERT`, `UPDATE` | `FUTURO` | Escrita **somente** via RPCs `cc_abrir_pendencia`/`cc_usar_pendencia`/`cc_encerrar_pendencia` (SECURITY DEFINER). Escrita direta de `anon`/`authenticated` revogada. | Crítico | Conferir `valor_saldo`/`valor_reservado`/`status` e a razão em `movimento_credito` após a operação. | `2026-08-11` / Fase 1 CC (aplicada) | Sem GRANT de INSERT/UPDATE a papéis da app. Uso único garantido por `FOR UPDATE`. RPCs confirmadas existentes em produção em 11/08/2026. |
| `public.conta_corrente_pendencias` | Qualquer campo | `DELETE` | `BLOQUEADO` | Pendências são resolvidas/canceladas/estornadas, nunca excluídas. | Crítico | Não aplicável. | Permanente | Sem policy de DELETE. |
| `public.movimento_credito` | Colunas estruturadas (`id_pendencia`, `tipo_evento`, `id_int_origem/destino`, `id_pagamento_destino`, `id_movimento_ref`, `motivo_evento`) | `READ` | `FUTURO` | Razão imutável de auditoria/reconciliação da Conta Corrente. | Alto | `select id, tipo_evento, tipo, valor, id_pendencia, id_movimento_ref from public.movimento_credito where id_pendencia = :id;` | `2026-08-11` / Fase 1 CC (aplicada) | Corrige a lacuna histórica: `movimento_credito` passa a constar nesta matriz. |
| `public.movimento_credito` | Lançamentos | `INSERT` | `FUTURO` | Inserção **somente** via RPCs. Escrita direta de `anon`/`authenticated` revogada; policy permissiva `geral` removida. | Crítico | Conferir o lançamento e a coerência com `valor_saldo` da pendência. | `2026-08-11` / Fase 1 CC (aplicada) | Acoplamento com a Fase 2 (refactor das rotas legadas usar-credito, resolver-diferenca, ajuste/estorno-credito, confirmar, pagamento-combinado) descrito em `business/CONTA-CORRENTE-FASE-1-PREPARACAO.md` §10. Em 11/08/2026 também foram confirmadas em produção as RPCs `mc_ajuste_avulso_criar`, `mc_ajuste_avulso_estornar`, `mc_confirmar_abatimento_legado` e `mc_usar_credito_avulso`, que indicam esse refactor em andamento/concluído — detalhamento dessas rotas não é escopo deste documento. |
| `public.movimento_credito` | Qualquer campo | `UPDATE`, `DELETE` | `BLOQUEADO` | Append-only por trigger. Correção/estorno só por lançamento compensatório (`ESTORNO`). | Crítico | Não aplicável. | Permanente | `estornarMovimentoCredito` (UPDATE lógico) descontinuada. Campos legados `cancelado*` congelados. Limpeza autorizada é fase separada. |
| `public.pagamentos_v2` | Colunas aditivas de reserva (`id_pendencia`, `valor_pendencia`, `reserva_estado`, `chave_reserva`) | `UPDATE` | `FUTURO` | Vínculo de reserva de débito, escrito **somente** via RPCs. Uma reserva ativa por pendência (índice parcial). Fluxos existentes de `pagamentos_v2` inalterados. | Crítico | `select id, id_pendencia, valor_pendencia, reserva_estado, chave_reserva from public.pagamentos_v2 where id_pendencia = :id;` | `2026-08-11` / Fase 1 CC (aplicada) | Apenas aditivo. Não altera criação/confirmação/cancelamento de cobrança existentes. |

## QR de Produção (OS)

> Fluxo público de transição de status de produção/expedição via QR Code impresso na OS (página `/os`, sem login). Migrations: `20260722_os_qr_producao.sql` (base, aplicada), `20260723_os_qr_grants_fix.sql` e `20260723_os_qr_digest_fix.sql` (patches aplicados) , `20260723_os_qr_multi_status.sql` (ampliação multi-status, aplicada em 23/07/2026) e `20260724_os_qr_motivo_opcional.sql` (motivo opcional em todas as transições). Feature flag `OS_QR_PUBLICO_ENABLED` desligada por padrão. Token opaco derivado por HMAC server-only (`OS_QR_TOKEN_SECRET`); o banco armazena somente hash SHA-256 (`extensions.digest`). Ordem única de bloqueio por OS: 1º `public.propostas` (`FOR UPDATE` por `id_int`), 2º `public.os_qr_tokens`.

| Tabela ou recurso | Campo ou escopo | Operações | Status | Motivo da decisão | Risco | Validação pós-gravação | Data/fase da liberação | Observações técnicas |
|---|---|---|---|---|---|---|---|---|
| `public.propostas` | `status_interno` (transições entre os 9 status de produção/expedição: `EM PRODUCAO`, `EM IMPRESSAO`, `EM IMPRESSAO / PENDENTE`, `EM ACABAMENTO`, `EM ACABAMENTO / PENDENTE`, `EXPEDICAO`, `A RETIRAR`, `EM TRANSITO`, `ENTREGUE`) | `UPDATE` | `FUTURO` | Transição pelo chão de fábrica **somente** via RPC `os_qr_transicionar` (SECURITY DEFINER): token válido + `FOR UPDATE` + checagem otimista do status esperado + classificação embutida (`osqr__classificar_transicao`). Motivo **opcional** em todas as transições — registrado em `os_status_log.motivo` e na timeline quando informado (patch `20260724_os_qr_motivo_opcional.sql`; a obrigatoriedade da v2 foi removida em 23/07/2026); `ENTREGUE` é terminal (sem transição posterior via QR); destino = atual rejeitado; a lista de destinos do frontend nunca é confiada. | Crítico | Conferir `status_interno` por `id_int` + linha em `public.os_status_log` (com `tipo_transicao` e `motivo`). | QR Produção v2 (ampliação preparada) | Próximo natural de `EXPEDICAO` definido pela cotação escolhida em `public.cotacao_frete` (serviço com `RETIRA`/`BALCAO` → `A RETIRAR`; transporte → `EM TRANSITO`; não informativo → nenhum natural). Ator técnico `qr_producao`. RPC `os_qr_avancar` (v1, só avanço linear) permanece durante a janela de deploy e será removida em migration de limpeza. |
| `public.os_qr_tokens` | Emissão/rotação/uso (hash, versão, revogação, janela de rate limit) | `INSERT`, `UPDATE` | `FUTURO` | Escrita **somente** via RPCs `os_qr_finalizar` (emissão/rotação — EXECUTE restrito a `service_role`, backend confiável) e `os_qr_avancar` (contadores de uso/rate). Emissão exige `pedidos.print_os`; rotação exige `pedidos.qr_rotacionar` — permissão e escopo revalidados dentro das funções para o `actor_uid` informado. | Crítico | `select id_int, versao, revoked_at, last_used_at from public.os_qr_tokens where id_int = :id_int;` | QR Produção v1 (preparada) | RLS habilitada **sem nenhuma policy** (nem SELECT). 1 token ativo por OS (índice único parcial). Token em claro jamais persiste. |
| `public.os_qr_tokens` | Qualquer campo | `DELETE` | `BLOQUEADO` | Tokens são revogados (`revoked_at`), nunca excluídos — trilha de revogação. | Crítico | Não aplicável. | Permanente | Sem policy de DELETE. |
| `public.os_status_log` | Trilha de tentativas e transições via QR (`status_anterior/novo`, `resultado`, `tipo_transicao`, `motivo` do operador, `origem`, ator técnico) | `INSERT` | `FUTURO` | Auditoria estruturada exigida pelo FLUXO-OFICIAL §11, gravada pelas RPCs definer do QR e, também, pela via direta autenticada da Expedição (ver seção "Expedição" abaixo). `tipo_transicao` (coluna aditiva da ampliação) classifica natural/pausa/retomada/salto/retorno/lateral/avanco_entrega. | Alto | `select * from public.os_status_log where id_int = :id_int order by id desc;` | QR Produção v1/v2 (preparada) | RLS habilitada **sem nenhuma policy**. Leitura administrativa futura terá gate próprio. |
| `public.os_status_log` | Qualquer campo | `UPDATE`, `DELETE` | `BLOQUEADO` | Trilha imutável. | Crítico | Não aplicável. | Permanente | Sem policies de escrita/remoção. |
| `public.propostas_chat` | Mensagem `SISTEMA` de avanço via QR (`autor_nome='QR Produção'`, setor `PRODUCAO`, `visivel_externo=false`) | `INSERT` | `FUTURO` | Visibilidade operacional na timeline, via helper `osqr__timeline` dentro da mesma transação do avanço. | Médio | Consultar mensagens do `id_int` após a transição. | QR Produção v1 (preparada) | Mesma família da linha existente de Chat Interno. |
| `service_role` (chave) | Uso restrito ao módulo server `os-qr-token.server.ts` (rotas de emissão/rotação) para executar `os_qr_finalizar` | Execução de RPC | `FUTURO` | `os_qr_finalizar` não concede EXECUTE a `anon`/`authenticated`; somente o backend confiável executa, com `p_actor_uid` revalidado na função. | Crítico | Auditar chamadas pelo retorno estruturado + `os_qr_tokens`. | QR Produção v1 (preparada) | Proibição de service_role no frontend permanece integral. Chave apenas em env server-only. |

## Expedição

> Módulo de execução da expedição no ERP interno (telas autenticadas, sem QR/sem público): peso aferido do pacote, volumes, transportadora definida no despacho, rastreio, prepostagem dos Correios e datas de pronto/despacho/entrega. O status oficial do pedido continua em `propostas.status_interno` (fluxo `EXPEDICAO` -> `A RETIRAR` | `EM TRANSITO` -> `ENTREGUE`, doc `FLUXO-OFICIAL-STATUS-PROPOSTAS.md` seção 6.13). Base de dados criada pela migration `20260815_expedicoes.sql` (Task 1 do plano de 18 tasks da Expedição).

| Tabela | Campo | Operações | Status | Motivo da decisão | Risco | Validação pós-gravação | Data/fase da liberação | Observações técnicas |
|---|---|---|---|---|---|---|---|---|
| `public.expedicoes` | Todos os campos operacionais (`tipo_frete`, `transportadora_nome`, `id_transportadora_cliente`, `peso_kg`, `qtd_volumes`, `tipo_volume`, `id_endereco_entrega`, `codigo_rastreamento`, `correios_id_prepostagem`, `correios_codigo_objeto`, `data_pronto`, `data_despacho`, `data_entrega`, `despachado_por`, `retirado_por`, `obs`) | `INSERT`, `UPDATE` | `LIBERADO` | Tabela nova (1 linha por `id_int`) para a execução da expedição: peso aferido, volumes, transportadora definida na hora, rastreio, prepostagem dos Correios e datas. Resolve a limitação de `propostas.peso` (`smallint`, estoura em ~32kg) e evita mutar `cotacao_frete` (histórico de cotação, não deve ser alterado). Liberado para `authenticated`; sem policy para `anon`. | Médio/Alto | `select id_int, tipo_frete, transportadora_nome, peso_kg, qtd_volumes, codigo_rastreamento, data_pronto, data_despacho, data_entrega from public.expedicoes where id_int = :id_int;` | `2026-08-15` / Expedição Fase 1 (base) | `DELETE` bloqueado (sem policy — linha de expedição não se apaga). `SELECT` também liberado para `authenticated`. `id_int` é `unique not null`; policies de `INSERT`/`UPDATE` exigem `id_int is not null`. Rastreio segue espelhado em `propostas_os.codigo_rastreamento` para as telas legadas. Migration: `supabase/migrations/20260815_expedicoes.sql`. |
| `public.expedicoes` | `peso_bruto_kg`, `pesos_volumes` (jsonb) — gravados pela aba **Revisão do boletim**, não pela tela de Expedição | `INSERT`, `UPDATE` (upsert por `id_int`) | `LIBERADO` | Peso bruto (com embalagem) é o número que vai para a NF-e e é grandeza distinta de `peso_kg` (aferido, usado na etiqueta e na prepostagem) — por isso coluna própria, não reaproveitamento. `pesos_volumes` existe porque a NF de múltiplos volumes exige o peso de cada um; só é gravada quando a quantidade é maior que 1, e a sobra de uma contagem maior anterior é descartada. Colunas nuláveis e aditivas: nenhuma coluna existente foi alterada. | Médio | `select id_int, qtd_volumes, tipo_volume, peso_bruto_kg, pesos_volumes from public.expedicoes where id_int = :id_int;` | `2026-08-16` / Revisão do boletim | Mesmas policies da linha acima (`authenticated`, sem `DELETE`). O upsert vem de `salvarRevisaoGeral` (`src/features/pedidos/services/revisao-expedicao.service.ts`); a liberação do pedido delega a transição de status a `marcarPronto`, sem caminho próprio para `status_interno`. Migration: `supabase/migrations/20260816_expedicoes_peso_bruto.sql`. |
| `public.expedicoes` | `modalidade_frete` (`RETIRA` \| `FOB` \| `CIF`) — declarada pelo expedidor no modal Despachar/Editar | `INSERT`, `UPDATE` (upsert por `id_int`) | `LIBERADO` | Modalidade comercial do frete = **quem paga**, dimensão ortogonal a `tipo_frete` (**por onde vai**). Coluna aditiva e nulável, com CHECK próprio; `expedicoes_tipo_frete_check` não foi alterado. Sem backfill: a modalidade nasce quando alguém a declara, porque o texto da cotação é ambíguo demais para inferi-la. **Nesta fase `CIF` é apenas rótulo** — não cota, não recota, não altera `propostas.valor_frete`/`valor_total` e não lança nada na Conta Corrente; a fase de recotação segue pendente de decisão do dono. | Médio | `select id_int, modalidade_frete, tipo_frete from public.expedicoes where id_int = :id_int;` + `select conname, pg_get_constraintdef(oid) from pg_constraint where conrelid='public.expedicoes'::regclass;` | `2026-08-18` / Modalidade de frete | Mesmas policies das linhas acima (`authenticated`, sem `DELETE`). Escrita por `despachar` e `salvarDadosExpedicao` (`expedicao-acoes.service.ts`). Nada em `cotacao_frete`, `propostas` ou Conta Corrente é tocado por este campo. Migration: `supabase/migrations/20260818_expedicoes_modalidade_frete.sql`. Regra de negócio em `docs/business/EXPEDICAO.md` §5.1. |
| `public.os_status_log` | Registro de transição gravado pela Expedição do ERP (`id_int`, status anterior/novo, `origem='EXPEDICAO_UI'`, ator, motivo) | `INSERT` | `LIBERADO` | A Expedição do ERP passa a registrar as próprias transições de status direto do client autenticado (fora do fluxo QR/RPC), identificadas por `origem='EXPEDICAO_UI'`. Policy aditiva à trilha já usada pelas RPCs `SECURITY DEFINER` do QR público (ver seção "QR de Produção (OS)" acima). | Alto | `select * from public.os_status_log where id_int = :id_int order by id desc;` | `2026-08-15` / Expedição Fase 1 (base) | Sem `SELECT`/`UPDATE`/`DELETE` para `authenticated`: trilha de auditoria e escrita-e-esquecida do lado do client. Policy exige `id_int is not null`. Coexiste com a policy `FUTURO` das RPCs do QR listada acima (origens distintas gravando na mesma tabela). Migration: `supabase/migrations/20260815_expedicoes.sql`. |
| `public.propostas` | `status_interno` — transições operadas pela Expedição (`EXPEDICAO` -> `A RETIRAR` \| `EM TRANSITO` -> `ENTREGUE`) | `UPDATE` | `LIBERADO` | Via módulo Expedição: transição feita com guarda de concorrência (`WHERE status_interno = <esperado>`) e log obrigatório em `os_status_log`. É mais uma via de escrita para o mesmo campo, ao lado da transição manual genérica (`EM TESTE`, seção "Propostas" acima) e da via QR (`FUTURO`, seção "QR de Produção (OS)"). | Crítico | Conferir `status_interno` por `id_int` e a linha correspondente em `public.os_status_log`. | `2026-08-15` / Expedição Fase 1 (decisão registrada) | A migration `20260815_expedicoes.sql` (Task 1) não altera policy de `public.propostas`; cria apenas `expedicoes` e a policy de `INSERT` em `os_status_log`. A chamada guardada de `UPDATE` em `status_interno` é implementada em task posterior do mesmo plano — esta linha registra a política já decidida para orientar essa implementação. Não usa RPC `SECURITY DEFINER`. |
| `public.propostas` + `public.expedicoes` + `public.os_status_log` | Escrita do RECEIVER do webhook dos Correios (`/api/correios/webhook`, **service-role**): `status_interno` (`EXPEDICAO` -> `EM TRANSITO` em postagem/coleta; -> `ENTREGUE` em entrega ao destinatário), `expedicoes.correios_ultimo_evento(_em)`, `expedicoes.data_entrega`, log com `origem='CORREIOS_WEBHOOK'` e `ator_tipo='SISTEMA'` | `UPDATE`, `INSERT` (log) | `LIBERADO` | Quem chama é o servidor dos Correios (sem sessão): autenticidade por HMAC (`x-correios-signature` com `CORREIOS_WEBHOOK_SECRET`); escrita via `SUPABASE_SERVICE_ROLE_KEY`, mesmo padrão do QR público (`os-qr-token.server.ts`). Transições guardadas por status esperado; evento sem objeto/pedido correspondente é ignorado com `200`. | Crítico | `select status_interno from propostas where id_int=:id;` + `select correios_ultimo_evento, correios_ultimo_evento_em, data_entrega from expedicoes where id_int=:id;` + linha `origem='CORREIOS_WEBHOOK'` em `os_status_log`. | `2026-08-17` / Webhook Correios | Segunda via server-side de `status_interno` (a primeira é o QR via RPC). Eventos gatilho: `PO-*`/`CO-1|15|16`/`CMT-0` (trânsito) e `BDE|BDI|BDR-1|67|68|70` (entrega). Colunas novas: migration `20260817_expedicoes_webhook_correios.sql`. Assinatura gerida por `scripts/correios-webhook.mjs`. |

## Observações de uso

- Esta matriz não substitui o checklist técnico de cada mudança.
- Antes de habilitar qualquer novo `UPDATE` ou `INSERT`, revisar:
  - RLS;
  - triggers;
  - views dependentes;
  - validação pós-gravação;
  - confirmação explícita na UI.
- Sempre que uma fase for concluída, atualizar:
  - status;
  - data/fase;
  - validação pós-gravação;
  - observações técnicas.

---

# Fonte da Verdade

Esta matriz representa a autorização oficial de leitura e escrita do Vibe no Supabase.

Sempre que uma nova operação de `INSERT`, `UPDATE` ou `DELETE` for liberada, bloqueada, colocada em teste ou transferida para fase futura, este documento deve ser atualizado junto com a implementação correspondente.

Nenhuma documentação secundária substitui esta matriz para decisões de escrita real no banco de dados.
