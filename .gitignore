# dependencies
node_modules/

# next
.next/
out/

# env
.env
.env.local
.env.*.local

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# backups
*.backup
*.sql
!supabase/migrations/**/*.sql
# Registros de manutencao de dados (o que foi executado + rollback). Precisam
# ser versionados: movimento_credito e conta_corrente_pendencias nao tem
# trigger de auditoria, entao estes arquivos sao o unico rastro da operacao.
!supabase/manutencao/**/*.sql
BACKUPS_SUPABASE/

# development scratch directories and build info
scratch/
*.tsbuildinfo
test-router.ts

# --- Rascunhos, diagnosticos e artefatos locais de sessoes de IA (nunca publicar) ---
# Dica: para arquivos descartaveis de teste/diagnostico, use a pasta scratch/ (ja ignorada acima).
/scratch_*
/diag_*
/diag[0-9]*
/diag*.mjs
/check_*
/test_[0-9]*.js
/test_list_*.js
/*.n8n
/Untitled-*
.claude/
.agents/
.mcp.json
